# Issue #193 设计与执行计划迭代审核

日期：2026-09-09。审核 agent：`/root/review_193_plan`。本记录由主代理整理，结论来自该 agent 的实际返回。

## 范围约束

只审核和完善已批准 R2 与 Issue #193 的执行计划，不实现功能。不得扩展到 #194、新依赖、新持久化格式、安装框架重写或新的安全机制。scope 外增强不作为阻塞；如必须扩大 scope 才能解决致命 P0/P1，向用户单独汇报。本轮未发现此类问题。

## 第一轮：REQUEST_CHANGES

发现两项 P2，均为 scope 内计划矛盾；原定位为计划 Task 6 第 401 行。

| 编号 | 问题与现有代码证据 | 最小修订 |
| --- | --- | --- |
| P2-1 | 无条件填入 ExpectedPreview 与 Task4 要求 Yes=true 冲突，普通 CLI 升级会被拒绝 | 内部 prepared 快照用独立 expected 参数传递；公开 Consent 不改写，无 yes 的普通升级仍复核快照 |
| P2-2 | installer_unix.go 的 ApplyPrepared 使用 start=false，而 Apply 使用 start=true；计划混用公共入口会丢失此差异 | 共享私有 applyWithReplacement 显式接收 start；两个入口保留原值 |

主代理核实代码和参数契约后修订 Task6，补充普通升级、陈旧目标、公开参数组合与服务启动策略验收，并同步设计 §5.1。没有增加功能范围。Task7/9/11 未发现需要新增范围的阻塞问题。

## 第二轮

**PASS，第二轮审核通过。**

同一 agent 已核实两项 P2 均修正：内部快照与公开 Consent 分离，普通升级无需 yes 而陈旧目标仍拒绝；ApplyPrepared 保持 start=false，直接安装保持 start=true。设计 §5.1、计划 Task6 与验收要求一致，未发现新的矛盾或剩余阻塞问题。

审核明确确认 scope 未扩大，无需新增功能、安全机制或 #194 内容；未发现需要扩大范围处理的 P0/P1。此结论仅表示设计与计划审核通过，不代表功能实现或功能测试通过。

## 追加审核：第一轮 NEEDS_REVISION

用户再次要求限定 scope 迭代审核，由 `/root/review_issue_193_design` 审查当前设计与计划。本轮不继续实现，已有代码保留。

| 级别 | 反馈 | 核实后的最小修订 |
| --- | --- | --- |
| P2 | Task8 先写纯文本警告，后续失败会污染 stderr 的错误 JSON | 同步内部 Warn 接口与锁外调用约束；文本即时、JSON 缓冲；执行/清理失败的风险合入单一 APIError envelope，增加验收步骤 |
| P2 | 按文件身份去重可能丢掉硬链接的另一个替换目录项 | 设计与接口明确按规范化目录项路径合并；不同硬链接分别绑定，Windows 同一目录项的别名规范化；补验收方向 |
| P2 | PATH 指向本机 Go 1.27.1，不能保证 1.26.5 | 执行起点显式设置 GOTOOLCHAIN=go1.26.5，核对 go version |

三项均为已批准 #193 范围内的一致性修订。未发现必须扩大 scope 的 P0/P1；未纳入 #194、安装框架重写或新的并发安全机制。新增验收步骤保持未勾选，不把文档修订视为测试通过。

## 追加审核：第二轮 PASS

同一 agent `/root/review_issue_193_design` 实际返回 **PASS**：三项 P2 均已修订，设计与计划一致，没有剩余文档阻塞，没有扩大 #193 scope，未发现需越界处理的 P0/P1。

主代理另验证三个文档的代码围栏、相对链接与行末空白，检查通过；执行起点的完整 PATH/GOTOOLCHAIN 命令实测输出 `go version go1.26.5 linux/amd64`。这仅证明文档检查及工具链选择，不代表新增实现验收通过。本轮只修改设计、计划和审核记录，未提交或推送。

## 实现阶段最终复审

继续已授权实施后，独立 reviewer 对实现与批准方案核对并闭环：离线已可信摘要的tag绑定、Windows同一目录项别名（含首次安装）、TUI角色展示与72×22确认按钮、远程Unix AIO在线helper选择均以范围内回归修正。PowerShell本地、远程及Go/Unix跨层审核分别PASS，没有未关闭的具体阻塞。

远程Unix AIO的解压路径不代表离线，设计§5.2同步明确online/offline经固定参数传递；本地离线不联网，固定候选不改变。未扩大#193 scope，没有需要越界处理的P0/P1。原生Windows/macOS与最终CI结论仍按执行计划记录，审核PASS不冒充平台测试通过。
