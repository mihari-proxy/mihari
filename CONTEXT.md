# Mihari

Mihari 管理本机 mihomo，并向本地用户提供一致的配置管理与运行控制。

## Language

**核心通道（Core Channel）**：mihomo 官方核心的发布系列，分为 stable 与 alpha。它与 Mihari 自身的 main/dev 发布通道不同。
_Avoid_: 将核心通道与 Mihari 应用通道混用。

**官方核心（Official Core）**：由 MetaCubeX/mihomo 官方发布的核心制品。官方来源不代表该制品已被 Mihari 逐版本验证兼容。
_Avoid_: 将官方核心等同于某个固定版本或兼容性保证。

**通道最新版本（Latest Channel Release）**：检查时对应核心通道的最新官方发布。它与已经安装或离线安装包内附的核心版本不同。
_Avoid_: 将最新版本与当前版本、内附版本混用。

**本地已有核心（Existing Local Core）**：已经部署在 Mihari 核心位置的可执行制品，可来自官方发布或有权部署者的自编译版本。它是否为官方制品与能否被本机接纳是不同问题。
_Avoid_: 将缺少官方来源证明等同于不能接纳。

**本次更新目标（Selected Core Release）**：一次核心更新开始时确定的通道发布及具体制品。上游随后发布新版不改变本次目标。
_Avoid_: 将本次目标等同于随时变化的通道最新版本。

**统一日志级别（Unified Logging Level）**：Mihari 与 mihomo 普通日志共同采用的记录阈值，在 System 的 Logging 区域显示为 Level。它与 Logs 页的显示筛选及实时日志订阅阈值是不同概念。
_Avoid_: 将 System 的 Level 与 Logs 页筛选混用。

**日志显示级别筛选（Log Display Level Filter）**：Logs 页中用户选择展示的一组日志级别；一条记录属于任一选中级别时即满足级别条件。它不改变统一日志级别或实时日志订阅阈值。
_Avoid_: 将多选显示级别理解为最低记录阈值。

**代理节点（Proxy Node）**：可作为代理连接出口的节点。节点名称用于展示，名称相同不表示两个来源中的节点必然是同一个对象。
_Avoid_: 将所有可显示的组成员统称为全局节点。

**节点身份（Node Identity）**：区分目标节点的身份，包含节点来源的区别。不同节点提供者中的同名节点是不同目标；名称相同不能证明来源相同。
_Avoid_: 以展示名称直接等同节点身份。

**同名合并项（Merged Name Entry）**：多个来源中的同名节点在界面上共用的一个名称项。该项的一次测速只代表实际被测试的某个节点，不保证代表代理组实际选中的来源。
_Avoid_: 将合并显示理解为底层节点已合并。

**代理组（Proxy Group）**：组织代理节点或其他代理组、并按选择或策略提供出口的集合。
_Avoid_: 将代理组与单个叶子节点混用。

**节点提供者（Proxy Provider）**：向内核提供一组代理节点的来源。它与 Mihari 管理的订阅配置不是同一个概念。
_Avoid_: 将 provider 与 Mihari 订阅条目直接等同。

**单节点测速（Node Latency Test）**：针对一个明确目标节点及测试地址测量连通延迟的操作。请求失败不必然表示目标节点无法联网。
_Avoid_: 网速测试、带宽测试。

**组测速（Group Latency Test）**：针对一个代理组执行成员延迟测试的操作。
_Avoid_: 将组测速与对该组当前出口执行一次单节点测速混用。

**诊断记录（Diagnostic Record）**：某次具体运行事件的诊断信息。同一种错误重复发生时，每次发生都是独立记录。
_Avoid_: 将内容相同的多次错误视为一次发生。

**诊断历史（Diagnostic History）**：可供本地用户回看的一组诊断记录，其中较早的记录可能已过期或被淘汰。
_Avoid_: 将诊断历史等同于完整、永久保存的文件日志。

**路由规则（Routing Rule）**：按匹配顺序判定连接应交给哪个目标处理的规则，包含规则类型、匹配内容和目标。
_Avoid_: 将一条规则的详情与它引用的整个规则集混用。

**规则集引用（RuleSet Rule）**：通过规则集名称引用一组匹配条件的路由规则。引用规则本身与规则集内部的条目是不同的查看对象。
_Avoid_: 将 RuleSet 的名称当作规则集的完整内容。

**连接链路（Connection Path）**：一条连接从发起应用、入站、路由决策、出站到访问目标的处理关系。处理阶段与策略组的选择关系不等同于实际网络中转跳数。
_Avoid_: 将每个处理阶段或代理组都称为网络一跳。

**已关闭连接记录（Closed Connection Record）**：Conns 曾观察为活动、随后不再出现在活动列表中的连接的历史记录。它不代表连接必然由用户主动关闭，也不构成完整的网络访问历史。
_Avoid_: 将已关闭连接记录统称为手动断开记录或完整访问审计。

**连接出站（Connection Outbound）**：连接经路由决策后采用的对外连接方式及相关远端信息，可以是代理或直连。
_Avoid_: 将出站与最终访问目标混用。

**连接目标（Connection Destination）**：应用希望访问的目的端，由已知的目标主机名、目标地址和端口描述。
_Avoid_: 将代理服务器地址直接视作应用访问目标。

**远端对端（Remote Peer）**：本地出站连接观察到或采用的远端地址，代理连接时通常是代理或传输端点，直连时可以与连接目标相同。
_Avoid_: 公网出口 IP、将远端对端固定等同最终目标。

**出站选择链（Outbound Selection Chain）**：一条连接所经过的代理选择关系，包含选中的出站及相关代理组。它不保证包含底层拨号代理的所有网络中转。
_Avoid_: 完整网络拓扑、逐跳网络探测结果。

**入站名称（Inbound Name）**：接收连接的入口标识，用于区分不同的默认入口或自定义监听入口。同一入口可以接收不同协议的连接。
_Avoid_: 将入口名称与入站类型、网络协议或网卡名称等同。

**守护进程启动时刻（Daemon Start Time）**：当前这次 Mihari daemon 进程开始运行的时刻。
_Avoid_: OS 服务安装时刻、TUI 打开时刻、核心启动时刻、把时长本身当成这个概念。

**核心启动时刻（Core Start Time）**：当前正被监督的 mihomo 进程开始运行的时刻。该进程被替换后，这个时刻指向新进程；当前没有 core 进程时，这个时刻不存在。
_Avoid_: daemon 启动时刻、本次 daemon 生命周期内第一次成功启动、把时长本身当成这个概念。

**核心重启次数（Core Restart Count）**：当前这次 daemon 生命周期内，core 进程被再次拉起的次数。第一次成功启动计为 0。自愿与非自愿的再次拉起都计入。
_Avoid_: 崩溃次数、启动次数、daemon 重启次数。
