# Mihari

Mihari 管理本机 mihomo，并向本地用户提供一致的配置管理与运行控制。

## Language

**统一日志级别（Unified Logging Level）**：Mihari 与 mihomo 普通日志共同采用的记录阈值，在 System 的 Logging 区域显示为 Level。它与 Logs 页的显示筛选及实时日志订阅阈值是不同概念。
_Avoid_: 将 System 的 Level 与 Logs 页筛选混用。

**代理节点（Proxy Node）**：可作为代理连接出口的节点。节点名称用于展示，名称相同不表示两个来源中的节点必然是同一个对象。
_Avoid_: 将所有可显示的组成员统称为全局节点。

**节点身份（Node Identity）**：区分目标节点的身份，包含节点来源的区别。不同节点提供者中的同名节点是不同目标；名称相同不能证明来源相同。
_Avoid_: 以展示名称直接等同节点身份。

**同名合并项（Merged Name Entry）**：多个来源中的同名节点在界面上共用的一个名称项。该项的一次测速只代表实际被测试的某个节点，不保证代表代理组实际选中的来源。
_Avoid_: 将合并显示理解为底层节点已合并。

**代理组（Proxy Group）**：组织代理节点或其他代理组、并按选择或策略提供出口的集合。
_Avoid_: 将代理组与单个叶子节点混用。

**节点提供者（Proxy Provider）**：向内核提供一组代理节点的来源。它与 Mihari 管理的订阅配置不是同一个概念。
_Avoid_: 将 provider 与 Mihari 订阅条目直接等同。

**单节点测速（Node Latency Test）**：针对一个明确目标节点及测试地址测量连通延迟的操作。请求失败不必然表示目标节点无法联网。
_Avoid_: 网速测试、带宽测试。

**组测速（Group Latency Test）**：针对一个代理组执行成员延迟测试的操作。
_Avoid_: 将组测速与对该组当前出口执行一次单节点测速混用。

**路由规则（Routing Rule）**：按匹配顺序判定连接应交给哪个目标处理的规则，包含规则类型、匹配内容和目标。
_Avoid_: 将一条规则的详情与它引用的整个规则集混用。

**规则集引用（RuleSet Rule）**：通过规则集名称引用一组匹配条件的路由规则。引用规则本身与规则集内部的条目是不同的查看对象。
_Avoid_: 将 RuleSet 的名称当作规则集的完整内容。
